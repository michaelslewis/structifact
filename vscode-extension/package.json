{
  "name": "structifact-validate",
  "displayName": "Structifact",
  "description": "Run Structifact's own `validate` and `discover` commands against files in this workspace and show the results as native VS Code UI.",
  "version": "0.0.1",
  "private": true,
  "publisher": "michaelslewis",
  "engines": {
    "vscode": "^1.85.0"
  },
  "scripts": {
    "test": "node test/extension.test.js"
  },
  "categories": ["Other"],
  "activationEvents": [
    "onCommand:structifact.validate",
    "onCommand:structifact.discoverDataset"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "structifact.validate",
        "title": "Structifact: Validate"
      },
      {
        "command": "structifact.discoverDataset",
        "title": "Structifact: Discover Dataset"
      }
    ],
    "configuration": {
      "title": "Structifact",
      "properties": {
        "structifact.cliPath": {
          "type": "string",
          "default": "structifact",
          "description": "Path to the Structifact CLI executable. You usually don't need to set this: if left unset, the extension automatically checks this workspace's own .venv/ or venv/ folder for a structifact binary before falling back to PATH. Set this explicitly only for an unusual setup (a differently-named or differently-located virtualenv)."
        }
      }
    }
  }
}
